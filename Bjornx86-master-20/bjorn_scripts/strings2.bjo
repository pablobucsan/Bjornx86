#use "libraries/user_libs/bstdio.bjo"

func void swap(ptr<str> s1, ptr<str> s2)
{
    *s1 = *s2;
}

func void main()
{
    str s1 = "hello";

    print(s1);
    
    str s2 = "hey";
    
    swap(&s1,&s2);

    print(s1);
}
