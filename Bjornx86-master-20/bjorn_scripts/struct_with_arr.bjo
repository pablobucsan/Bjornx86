#use "libraries/user_libs/bstdio.bjo"


object A 
{
    uint8 count;
    uint8[3] arr;
}

func void main()
{
    A a;
    a.count = 3;
    for (uint8 i = 0; i < a.count; i++)
    {
        a.arr[i] = i;
        print(a.arr[i]);
    }
}