---- 4th Feb

1. RVALUE CALCULATED BEFORE LVALUE
2. NOT EMITTING imul, r,r,1
3. USING [rax + field.offset]  when possible
4. COMPARING TYPES THROUGH POINTERS (TYPEA == TYPEB)

---- 7th Feb
1. Fixed external fragmentation issue in malloc(), when a heap has enough space total but not in blocks, we would still get into that heap, and since no block can satisfy the request we would return null instead of advance to next heap / create new heap
2. Type system was scattered and unclear what functions did add the types to the table, cleaned that up a lil bit
3. Allowed sizeof() to take an expression as argument so now we can do sizeof(array)/sizeof(array[0])

---- 8th Feb
1. Added forward declarations of functions, enums, objects and classes
2. Added error msg for: An object/class can't have a field that is the type (referenced by value) of a forwarded struct since we don't know the size of the struct yet. Use a pointer
3. Better highlighting of patterns, now Enum,object,class,union are highlighted the same as built-in types

---- 13th Feb
1. Collecting strings before buildData() so if data references strings we already have the address
2. Fixed for loop reassignment expr to take both expressions and reassignments
3. Fixed LOGICAL AND/OR operations not setting the result to either 1/0 and instead performing bit and
4. Better varargs with boxed types
5. sprintf and vsprintf
6. Added zalloc() to bstdlib for fucking sanity after fixing a but that had to do with that
7. Added 'break' and 'continue', changed foreach lowering to while --> foreach lowering to for so we can have the labels
   for reassignment and can jmp to those in case of 'continue'
8. Fixed cloneAST() lacking cloning for PTR_METHOD_DISPATCH and METHOD_DISPATCH 
9. Added Errors for when performing any type method dispatch on an undefined class
10. Fixed overwritting memory when dealing with structs/classes by value with non power of 2 size, call it N,
    register holding the data is bigger than total size used, call it M, and when storing we were storing 
    M bytes, therefore overwritting the next M - N bytes in the stack 

---- 23rd Feb
1.  Changed instructions like: "mov r64, label" to "lea r64, [label]".
    This was only happening when loading strings but had no easy way to encode it because it would mean moving into 
    r64 an immediate value (the address of label). Now with "lea r64, [label]" we can use rip relative addressing
2.  Was lacking stacking offsets when accessing fields by value, now we do:
    'Operand *op_result = createOperandWithDisplacement(isTypeUnsignedInt(field->smi->type), base->displacement + field->smi->offset ,base->linkedToRegister);'
    For PTR_FIELD_ACCESS is not needed because we load the address already
3. Also lacking stacking offsets when an object would have an array defined like:
    object Foo{
        uint8 count;
        uint16[size] arr;
    }
    f.arr --> was at the wrong offset!!

4. Changed the initial mov rdi, [rsp] for command line arguments to main function
   to use rbp instead (with an initial mov rbp, rsp) so we dont have to encode SIB byte
   for rsp addressin (this was the only use os [rsp +- disp] in the entire compiler)
5. Lacked NODE_RETURN as valid is_stmt() therefore not clearing the registers when accessing a return node and causing an issue
   when having an empty expr like:
    ptr_field_access->field;    ---> may use rax
   followed by a return:
    return x;                  ----> MUST use rax
   
   but since return wasn't considered a statement, it wasn't clearing the registers and since there was no current spill report -> crash 
   Fix: Add return as a valid stmt as it should

6. Fixed opening files with only group permissions so users used to get none => cant open files 
   Added O_TRUNC flag for overwritng files

7. Lacking elseif copy node, fixed that.
8. Wasn't ending the type with "\0" bruh
9. &(base.field) was only calculating &base, have to add the offset to the register linked to base 
10. Wasnt copying 'ast->is_stmt' bruh