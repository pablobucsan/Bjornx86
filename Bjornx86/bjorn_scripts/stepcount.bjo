


func uint32 stepcount(uint8 n)
{
    if (n == 0) { return 1; }
    if (n < 0) { return 0; }

    return stepcount(n - 1) + stepcount(n - 3) + stepcount(n - 5);
}

func void main()
{
    uint32 x = stepcount(30);
}
