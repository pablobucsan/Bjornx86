
#use "libraries/user_libs/bstdio.bjo"



func uint16 foo(uint16 x, uint16 y)
{
    uint16 z = x + y;

    return z;
}

func void main()
{
    uint16 x = 3;
    uint16 y = 4;

    uint16 z = foo(x, y);

    print(z);
}