#use "libraries/user_libs/bstdio.bjo"

func uint32 mc91(uint32 n)
{
    if (n > 100)
    {
        return n - 10;
    }

    else 
    {
        return mc91(mc91(n + 11));
    }
}


func void main()
{
    uint32 n = 50;

    print(mc91(n));
}